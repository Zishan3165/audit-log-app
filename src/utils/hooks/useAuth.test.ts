import { useAuth } from './useAuth';

describe('Test useAuth hook', () => {
  it('shows "Deleted" when time parameter is DELETE', () => {
    // const { auth, saveAuth, removeAuth } = useAuth();
    // expect(auth).toBeTruthy();
    // expect(saveAuth).toBeTruthy();
    // expect(removeAuth).toBeTruthy();
  });
});
